var assert = require('assert');

describe('todos', () => {
    describe('get all todos', () => {
        it('should return a list of all the todos', ()=>{
            assert.equal(1,1);
        })
    })
})